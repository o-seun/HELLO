<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h2>Variable</h2>
    <?php
    $name = "egoing";
    echo "In the example above, ".$name." notice that we did not have to tell ".$name." PHP which data type the variable is.
    PHP automatically associates a data type to the variable, ".$name." depending on its value. Since the data types are not set ".$name." in a strict sense, you can do things like adding a string to an integer without causing an error.
    In PHP 7, type declarations ".$name." were added. ".$name." This gives an option to ".$name." specify the data type expected when declaring a function, ".$name." and by enabling the strict requirement, it will throw a on a type mismatch.
    You will learn more ".$name." about strict and non-strict requirements, and data type declarations in the PHP Functions chapter. by ".$name;
     ?>
  </body>
</html>
